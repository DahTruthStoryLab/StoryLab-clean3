version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - npm ci || npm install
    build:
      commands:
        - cd client
        - npm run build
  artifacts:
    baseDirectory: client/dist
    files:
      - '**/*'
  cache:
    paths:
      - client/node_modules/**/*
redirects:
  - source: "/<*>"
    target: /index.html
    status: "200"
